import {Component,inject} from '@angular/core';import {CommonModule} from '@angular/common';import {ApiService,Agent} from '../../core/api.service';@Component({selector:'app-agents',standalone:true,imports:[CommonModule],templateUrl:'./agents.component.html'})export class AgentsComponent{api=inject(ApiService);items:Agent[]=[];ngOnInit(){this.api.agents().subscribe(x=>this.items=this.api.list<Agent>(x))}}
