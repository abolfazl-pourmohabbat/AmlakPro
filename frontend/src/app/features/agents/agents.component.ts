import {Component,inject} from '@angular/core';import {CommonModule} from '@angular/common';import {ApiService,Agent,SiteSettings} from '../../core/api.service';@Component({selector:'app-agents',standalone:true,imports:[CommonModule],templateUrl:'./agents.component.html'})export class AgentsComponent{api=inject(ApiService);items:Agent[]=[];settings:SiteSettings|null=null;ngOnInit(){this.api.siteSettings().subscribe({next:v=>this.settings=v});this.api.agents().subscribe(x=>this.items=this.api.list<Agent>(x))}}
